#include<iostream>

using namespace std;

int main(int argc, char *argv[])
{
  vector<int> v;
  int max;
  while(cin >> x && x != 0)
  {
    v.push_back(x);
  }

  for (int i = 0; i < int(v.size()); ++i) {
        
  }
  return 0;
}
